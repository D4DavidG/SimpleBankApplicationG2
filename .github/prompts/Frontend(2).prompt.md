Following the blueprint found in `Simple_Bank_Application_Project.md`, make a plan to build the frontend. Use the tech stack specified in `Simple_Bank_Application_Project.md`; if none is specified, default to plain HTML/CSS/JS. If `Simple_Bank_Application_Project.md` or `AGENTS.md` cannot be found, stop and ask the user for clarification before proceeding.

Include specific steps in the plan showing how Copilot Business can assist with frontend development tasks (e.g., code generation, test writing).

Since this is a practice project, limit the plan to no more than 3-5 core features, and keep the plan document under 300 words. Keep the plan concise so it can be easily explained in a presentation.

If the plan involves any Python-based tooling or scripts, remember to follow AGENTS.md and activate the venv before running them. Otherwise, this step does not apply. For example:
```bash
source .venv/bin/activate && python3 -m unittest -v
```